/**
 * esponsible for reading in all images in a directory (assume only image files are in the directory),
 * storing the images in an ArrayList,
 * applying filters to all images stored in an Album,
 * and for writing filtered images to an output directory
 */
public class Album implements ImageReader, ImageWriter{

    @Override
    public void read(String fileName) {

    }

    @Override
    public void write(String fileName) {

    }
}
